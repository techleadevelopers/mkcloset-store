export class AppConstants {
  static readonly MIN_PRODUCT_WEIGHT_KG = 0.1; // 100g
  static readonly MIN_CORREIOS_LENGTH_CM = 16;
  static readonly MIN_CORREIOS_WIDTH_CM = 11;
  static readonly MIN_CORREIOS_HEIGHT_CM = 2;
  static readonly FREE_SHIPPING_THRESHOLD = 199.00; // Valor para frete grátis
  static readonly DEFAULT_PRODUCT_WEIGHT_KG = 0.3; // 300g
  static readonly DEFAULT_PRODUCT_DIMENSION_CM = { length: 20, width: 15, height: 5 };

  // Outras constantes...
}